export const DEPLOY_TOKEN_PROMPT = 
`This tool will deploy a token (ERC-20) contract onchain from the wallet. 
It takes the name of the token, the symbol of the token, the number of decimals, and the initial supply as inputs.`; 